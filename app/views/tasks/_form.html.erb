<%= form_with(model: task, local: true) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>
  
  <% f.lavel :status, "ステータス" %>
  <% f.text_field :stasus %>

  <%= f.submit '投稿' %>
<% end %>